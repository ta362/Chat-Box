import { SerialPost } from '../types';

/**
 * Auto-publish engine disabled: pure real-user board.
 */
export function startAutoPublishEngine() {
  // No-op
}

export function stopAutoPublishEngine() {
  // No-op
}

export async function checkAndBackfillOfflinePosts(_existingPosts?: SerialPost[]) {
  // No-op
}
